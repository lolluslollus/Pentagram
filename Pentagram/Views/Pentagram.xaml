<uctl:ObservableControl
	xmlns:uctl="using:Utilz.Controlz"
    x:Class="Pentagram.Pentagram"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pentagram"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="LightGray">
		<Line X1="{x:Bind MinX, Mode=OneWay}" X2="{x:Bind MaxX, Mode=OneWay}" Y1="{x:Bind MinY0, Mode=OneWay}" Y2="{x:Bind MinY0, Mode=OneWay}" Stroke="Black"/>
		<Line X1="{x:Bind MinX, Mode=OneWay}" X2="{x:Bind MaxX, Mode=OneWay}" Y1="{x:Bind MinY1, Mode=OneWay}" Y2="{x:Bind MinY1, Mode=OneWay}" Stroke="Black"/>
		<Line X1="{x:Bind MinX}" X2="{x:Bind MaxX}" Y1="{x:Bind MinY2}" Y2="{x:Bind MinY2}" Stroke="Black"/>
		<Line X1="{x:Bind MinX}" X2="{x:Bind MaxX}" Y1="{x:Bind MinY3}" Y2="{x:Bind MinY3}" Stroke="Black"/>
		<Line X1="{x:Bind MinX}" X2="{x:Bind MaxX}" Y1="{x:Bind MinY4}" Y2="{x:Bind MinY4}" Stroke="Black"/>
	</Grid>
</uctl:ObservableControl>
